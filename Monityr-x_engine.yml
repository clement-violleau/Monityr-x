---

#------------------------------------------------------------------------------
# YAML Aliases to simplify maintenance

anchorLinks:
  - link: &grafana-home
      url: "https://grafana.com/"
      params: "time"
  - flowAnimation: &flow-slow
      thresholdOffValue: 2
      thresholdLwrValue: 10
      thresholdLwrDurationSecs: 1
      thresholdUprValue: 100
      thresholdUprDurationSecs: 0.1

#------------------------------------------------------------------------------
# Panel Config

test:
  testDataBaseOffset: 1
  
cellIdPreamble: "cell-"
gradientMode: "hue"
cells:
  # ---------- ALGORILLA LOGS ----------
  algorilla_logs_status:
    dataRef: "algorilla_logs"
    label:
      separator: "cr"
      units: "short"
    fillColor:
      thresholds:
        - {color: "semi-dark-red", level: 0}
        - {color: "semi-dark-yellow", level: 100}
        - {color: "semi-dark-green", level: 1000}
  # ---------- ALGORILLA ALARMS ----------
  algorilla_alarms_status:
    dataRef: "algorilla_alarms"
    label:
      separator: "cr"
      units: "short"
    fillColor:
      thresholds:
        - {color: "semi-dark-red", level: 0}
        - {color: "semi-dark-yellow", level: 100}
        - {color: "semi-dark-green", level: 1000}
  # ---------- CORALINE ALARMS ----------
  coraline_alarms_status:
    dataRef: "coraline_alarms"
    label:
      separator: "cr"
      units: "short"
    fillColor:
      thresholds:
        - {color: "semi-dark-red", level: 0}
        - {color: "semi-dark-yellow", level: 100}
        - {color: "semi-dark-green", level: 1000}
  # ---------- CONFIG ----------
  config_status:
    dataRef: "config"
    label:
      separator: "cr"
      units: "short"
    fillColor:
      thresholds:
        - {color: "semi-dark-red", level: 0}
        - {color: "semi-dark-green", level: 1}
        


